#include <Arduino.h>
#include <decode.h>
const int RECV_PIN = 2;
REC RCVPIN(RECV_PIN); 
void setup() {
  // put your setup code here, to run once:
  Serial.begin(230400);
  REC::begin();

}

void loop() {
  // put your main code here, to run repeatedly:
  
}
